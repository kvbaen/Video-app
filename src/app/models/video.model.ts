export interface Video {
    id: string,
    title?: string,
    thumbnailUrl?: string,
    viewCount?: string,
    likeCount?: string,
    videoUrl?: string,
    publishedAt?: Date,
    favourite?: boolean

}